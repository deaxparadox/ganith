[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ganith"
version = "0.0.1"
requires-python = ">=3.10"
authors = [{ name = "DEAXparadox" }]
readme = "README.rst"
dependencies = [
    "ipython>=8.14.0"
]

[project.scripts]
ganith = 'ganith:ganith'
